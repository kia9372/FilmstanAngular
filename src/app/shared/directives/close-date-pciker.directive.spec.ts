import { CloseDatePcikerDirective } from './close-date-pciker.directive';

describe('CloseDatePcikerDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseDatePcikerDirective();
    expect(directive).toBeTruthy();
  });
});
